<style is:global>
  @media (min-width: 1280px) {
    [data-menu-viewport] {
      width: min(1120px, calc(100vw - 40px)) !important;
    }

    [data-menu-shell] {
      height: min(29.5rem, calc(100vh - 5.75rem));
    }

    [data-menu-panel] {
      grid-template-columns: minmax(0, 1.45fr) minmax(360px, 0.95fr) !important;
      gap: 1.25rem !important;
    }

    [data-menu-panel] > :first-child {
      max-height: 100%;
      overflow: hidden;
      padding-top: 1.25rem;
      padding-bottom: 1.25rem;
    }

    [data-menu-panel] > :first-child > :last-child {
      display: grid !important;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 0.35rem 0.65rem;
      align-items: start;
    }

    [data-menu-panel] > :first-child > :last-child .navigation-menu-link {
      min-height: 4.75rem;
      align-items: flex-start;
      gap: 0.75rem;
      padding: 0.7rem;
    }

    [data-menu-panel] > :first-child > :last-child .navigation-menu-link > :first-child {
      width: 2.75rem;
      height: 2.75rem;
      margin-top: 0.05rem;
    }

    [data-menu-panel] > :first-child > :last-child .navigation-menu-link > :last-child {
      min-width: 0;
      font-size: 0.925rem;
      line-height: 1.25rem;
    }

    [data-menu-panel] > :first-child > :last-child .navigation-menu-link > :last-child > span {
      overflow: visible !important;
      white-space: normal !important;
      text-overflow: clip !important;
    }

    [data-menu-panel] > :first-child > :last-child .navigation-menu-link > :last-child > span:first-child {
      line-height: 1.2rem;
    }

    [data-menu-panel] > :first-child > :last-child .navigation-menu-link > :last-child > span:last-child {
      margin-top: 0.15rem;
      font-size: 0.8rem;
      line-height: 1.05rem;
    }
  }
</style>
